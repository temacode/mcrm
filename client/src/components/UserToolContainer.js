import { connect } from 'react-redux';
import UserTool from './UserTool';

let mapStateToProps = state => {
    return({

    });
}

let mapDispatchToProps = dispatch => {
    return({

    });
}

const UserToolContainer = connect(mapStateToProps, mapDispatchToProps)(UserTool);

export default UserToolContainer;